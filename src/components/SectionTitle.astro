---
interface Props {
  title: string;
  subtitle: string;
}

const { title, subtitle } = Astro.props;
---

<div class="text-center pb-12">
  <h2 class="text-2xl font-semibold text-hr-primary">{title}</h2>
  <p class="text-hr-primary-light">{subtitle}</p>
</div>
